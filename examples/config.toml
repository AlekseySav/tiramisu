[[session]]
  root = "$HOME/.dotfiles"
  name = "dotfiles"
  [[session.window]]
    name = "dotfiles"
    command = "nvim ."
  [[session.window]]
    name = "shell"

[[session]]
  root = "$HOME/src/(*)"
  name = "src/$1"
  [[session.window]]
    name = "$1"
    command = "nvim ."
  [[session.window]]
    name = "shell"

[[session]]
  root = "$HOME/tmp/(*)"
  name = "tmp/$1"
  [[session.window]]
    name = "$1"
    command = "nvim ."
  [[session.window]]
    name = "shell"

[logger]
level = "trace"
